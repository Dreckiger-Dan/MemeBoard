<Window x:Class="MemeBoard.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MemeBoard"
        mc:Ignorable="d"
        Title="Settings" Height="450" Width="800" Icon="res/lul.ico">
    <Grid>
        <GroupBox Header="Memes" Height="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel x:Name="memeSettings" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" />
                <Button x:Name="Add" Content="Add" Grid.Column="0" Grid.Row="2" Click="Add_Click" />
                <Button x:Name="Save" Content="Save" Grid.Column="1" Grid.Row="2" Click="Save_Click" />
            </Grid>
        </GroupBox>
        
    </Grid>
</Window>
